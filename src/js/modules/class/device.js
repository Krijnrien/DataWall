export default class Device {

    constructor(x, y, floor, userType, hash) {
        this.x = x;
        this.y = y;
        this.floor = floor;
        this.userType = userType;
        this.hash = hash;
    };
}
